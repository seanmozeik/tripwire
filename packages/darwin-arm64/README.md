# `@seanmozeik/tripwire-darwin-arm64`

Apple Silicon native executable for [`@seanmozeik/tripwire`](https://www.npmjs.com/package/@seanmozeik/tripwire).

Install the main package instead. It selects this optional executable on macOS arm64 and uses the portable Bun bundle elsewhere.
